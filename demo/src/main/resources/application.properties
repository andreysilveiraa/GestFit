spring.application.name=demo

# Configura��o Supabase PostgreSQL
spring.datasource.url=jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:6543/postgres?sslmode = require
spring.datasource.username=postgres.hdcdhunlnwfsfbtpkthj
spring.datasource.password=UGSfk0AOgYSe2JMX
spring.datasource.driver-class-name=org.postgresql.Driver

# Configura��es Hibernate/JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Desabilitar H2
spring.h2.console.enabled=false

# --- CONFIGURA��ES DE CONEX�O DO HIKARI (SUPABASE) ---
# Tempo m�ximo que o Spring vai esperar o Supabase responder (30 segundos)
spring.datasource.hikari.connection-timeout=30000

# Tempo m�ximo que uma conex�o pode ficar ociosa no pool
spring.datasource.hikari.idle-timeout=600000

# Tempo m�ximo de vida de uma conex�o no pool
spring.datasource.hikari.max-lifetime=1800000

# N�mero m�ximo de conex�es abertas simultaneamente
spring.datasource.hikari.maximum-pool-size=10

# Testa se a conex�o com o Supabase ainda est� viva antes de usar
spring.datasource.hikari.connection-test-query=SELECT 1